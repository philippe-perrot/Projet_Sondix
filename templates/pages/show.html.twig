{% extends 'base.html.twig' %}

{% block title 'QCM - Sondix' %}

{% block body %}

	<div class = "container mt-4">
		<div class = "row">
			<div class = "col-md-8">
				<h1>{{ qcm.NomQCM }}</h1>
			</div>
			<div class = "col-md-4">
				<a href = "{{ path('response', {id: qcm.IdQcm, slug: qcm.slug}) }}"><button type="button" class="btn btn-primary">Répondre</button></a>
			</div>
		</div>
	</div>
	
{% endblock %}